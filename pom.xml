<properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
</properties>

<build>
<plugins>
    <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
            <!-- Bruk kun release, ikke source/target samtidig -->
            <release>17</release>
            <!-- Tving AV preview uansett hva som settes andre steder -->
            <enablePreview>false</enablePreview>
        </configuration>
    </plugin>

    <!-- (valgfritt, men fint) pin surefire-versjon uten preview-argLine -->
    <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
    </plugin>
</plugins>
</build>